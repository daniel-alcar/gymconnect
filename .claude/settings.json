{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "statusMessage": "Rodando testes unitários antes de iniciar o app...",
            "timeout": 60,
            "command": "$json = $input | ConvertFrom-Json; $cmd = $json.tool_input.command; if ($cmd -match 'flutter run') { Push-Location 'C:\\Users\\ryan.doriguetto\\Downloads\\GymConnect\\mobile'; $result = flutter test test/unit/ 2>&1 | Out-String; $exitCode = $LASTEXITCODE; Pop-Location; if ($exitCode -ne 0) { @{continue=$false; stopReason=\"Testes unitarios falharam. Corrija os erros antes de executar o app:`n$result\"} | ConvertTo-Json -Compress; exit 1 } }"
          }
        ]
      }
    ]
  }
}
